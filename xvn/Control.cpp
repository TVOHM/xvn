#include "Control.h"

Control::Control() : mIsInitialised(false) { }

bool Control::initialise(const xml_node& node, const xml_node& game)
{
}

bool Control::conditionsMet(const map<string, string>& variables)
{
}